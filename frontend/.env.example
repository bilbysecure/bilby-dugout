# Base URL of the SlimPHP API (include the /api/v1 prefix)
VITE_API_BASE=http://localhost:8080/api/v1

# Local-only: show the "Dev sign-in" buttons that call /auth/dev-login (no Azure).
# Leave false/unset in any shared or production build.
VITE_DEV_LOGIN=true
